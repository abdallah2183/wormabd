flask>=2.0.1
python-dotenv>=0.19.0
llama-cpp-python>=0.2.0
diffusers>=0.24.0
torch==2.2.0      # تم التراجع لإصدار مستقر يحل مشكلة pytree
transformers==4.37.2 # إصدار متوافق مع torch 2.2.0
accelerate>=0.20.0
safetensors>=0.3.1
requests>=2.28.0
pillow>=9.0.0
numpy==1.26.4     # تم تقييد الإصدار ليتجنب التعارض مع NumPy 2.x
gunicorn>=22.0.0
```eof

---

## 🚀 الإجراءات المطلوبة الآن

1.  **عدّل `requirements.txt`:** قم بنسخ المحتوى أعلاه بالكامل ولصقه في ملف **`requirements.txt`** على جهازك، واستبدل كل ما فيه.
2.  **الرفع النهائي:** قم برفع التغييرات إلى GitHub لبدء عملية النشر الأخيرة:

    ```bash
    git add requirements.txt
    git commit -m "Ultra-final Fix: Locked torch, transformers, and numpy versions to ensure full deployment compatibility."
    git push origin main
    ```

**إذا نجح الرفع (وهذا يجب أن يحل كل مشاكل التوافق المعروفة)، راقب سجلات Render. يجب أن ترى الخدمة تتحول إلى "Live" (قيد التشغيل) بنجاح!**