flask>=2.0.1
python-dotenv>=0.19.0
llama-cpp-python>=0.2.0
diffusers>=0.24.0
torch>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0
safetensors>=0.3.1
requests>=2.28.0
pillow>=9.0.0
numpy>=1.24.0   
gunicorn>=22.0.0 # <--- **الاضافة الضرورية الجديدة**
```eof

---

### 🚀 الخطوات المطلوبة الآن

1.  **عدّل `requirements.txt`:** قم بنسخ المحتوى أعلاه ولصقه في ملف **`requirements.txt`** على جهازك.
2.  **عدّل `app.py`:** تأكد من أنك قمت بتحديث ملف **`app.py`** بالكامل كما أرسلته لك في الرد السابق (وهو التعديل الذي أزال كتلة `if __name__ == '__main__':` وتكرار `app = Flask(__name__)`).
3.  **الرفع النهائي:** قم برفع التغييرات إلى GitHub لبدء عملية النشر النهائية:

```bash
git add requirements.txt app.py
git commit -m "Final fix: Added gunicorn and optimized app.py for Render deployment."
git push origin main